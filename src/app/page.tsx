/**
 * NeuroStream Reader - Home Page
 *
 * Main application page with the RSVP reader.
 */

import { Reader } from '@/components/reader/Reader';

export default function HomePage(): React.ReactNode {
  return (
    <main>
      <Reader />
    </main>
  );
}
